<%= form_with(model: post, local: true) do |form| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

   <div class="form-group">
    <%= form.label :title, 'Tipo de delito' %> <br>
    <%= form.text_field :title, autofocus: true, required: true, :class => 'form-control' %>
  </div>

  <div class="field">
    <%= form.label :body, 'Barrio' %>
    <%= form.text_field :body, required: true, :class => 'form-control', :row => '3' %>
  </div>

  <div class="field">
    <%= form.label :text, 'Informe los hechos' %>
    <%= form.text_area :text, required: true, :class => 'form-control', :row => '3' %>
  </div>

  <!-- <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div> -->

   <div class="text-center mt-3">
    <%= form.submit 'Denunciar', :class => 'btn btn-primary' %>
  </div>
<% end %>

