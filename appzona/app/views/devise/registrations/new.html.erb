<body class="bg-light">

<h2 class="mt-5">Registrarse</h2>

  <p class="text-muted">Registrarse en Mi Zona es rápido y sencillo. Estás a menos de un minuto de ayudar a mejorar la seguridad ciudadana de su localidad.</p>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name), :html => {:class => 'form-signin' }) do |f| %>
 
  <%= devise_error_messages! %>

  <div class="form-row">
    <div class="form-group col-md-6">
      <%= f.text_field :firstname, autofocus: true, required: true, :class => 'form-control', :placeholder => 'Nombre' %>
    </div>
    <div class="form-group col-md-6">
     <%= f.text_field :lastname, required: true, :class => 'form-control', :placeholder => 'Apellido' %>
    </div>
  </div>
  <div class="form-row">
    <div class="form-group col-md-4">
      <%= f.select(:idtype, [['Tipo de documento', ''],['Cédula de Ciudadanía', 'CC'],['Cédula de Extranjería', 'CE'], ['Pasaporte','PS'], ['Registro Civil', 'RC'], ['Tarjeta de identidad','TC']], {}, {:class => 'custom-select mr-sm-2'}) %>
    </div>
    <div class="form-group col-md-8">
     <%= f.text_field :idnumber, autofocus: true, :class => 'form-control', :placeholder => 'Número de identificación' %>
    </div>
    </div>
  <div class="form-row">
   <div class="col-md-4 mb-3">
      <%= f.email_field :email, required: true, autofocus: true, autocomplete: "email", :class => 'form-control', :placeholder => 'Correo electrónico' %>
    </div>

    <div class="col-md-4 mb-3">
      <% if @minimum_password_length %>
      <% end %>
      <%= f.password_field :password, autocomplete: "off", :class => 'form-control', :placeholder => 'Introduce tu contraseña'  %>
      <small class="form-text text-muted">&nbsp;<%= @minimum_password_length %> caracteres como mínimo</small>
    </div>

      <div class="col-md-4 mb-3">
        <%= f.password_field :password, autocomplete: "off", :class => 'form-control', :placeholder => 'Confirmar contraseña'  %>
      </div>
    </div>
    <div class="text-center">
      <%= f.submit 'Registrarse', :class => 'btn btn-primary' %>
    </div>
   <% end %>
  <div class="text-center mt-3">
    <%= render "devise/shared/links" %>
  </div>

</body>