<div class="text-center">
<h2 class="mt-5 my-4">Registrarse</h2>

  <p class="text-muted">Registrarse en Mi Zona es rápido y sencillo.<br>
  Estás a menos de un minuto de ayudar a <br>
mejorar la seguridad ciudadana de su localidad.</p>

<%= form_for(resource, as: resource_name, :class => 'form-signin', url: registration_path(resource_name)) do |f| %>
 
  <%= devise_error_messages! %>

  <div>
    <%= f.email_field :email, required: true, autofocus: true, autocomplete: "email", :class => 'form-control form-signin-pass', :placeholder => 'Correo electrónico' %>
  </div>

  <div>
    <% if @minimum_password_length %>
    <!--<em>(<%= @minimum_password_length %> characters minimum)</em>-->
    <% end %>
    <%= f.password_field :password, autocomplete: "off", :class => 'form-control form-signin-pass', :placeholder => 'Introduce tu contraseña'  %>
  </div>

  <div>
    <%= f.password_field :password, autocomplete: "off", :class => 'form-control form-signin-email', :placeholder => 'Confirmar contraseña'  %>
  </div>

  <div class="actions">
    <%= f.submit 'Registrarse', :class => 'btn btn-lg btn-primary btn-block' %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
</div>